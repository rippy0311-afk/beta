# Beta — 1,000件アイデア設計カタログ

このカタログは、重複を避けるために「世界 × 発火条件 × 変化」をID化した設計母集団です。各項目には実装経路を記し、実装済みと候補を分けます。

- 設計済み: 1,000件
- 実装済み・進捗反映: 38件（詳細は `docs/idea-implementation-progress.md`。このカタログは後からIDを付けた設計母集団のため、既存実装をID順に対応付けていません）
- 候補・素材待ち: 962件

| ID | アイデア | 実装経路 | 状態 |
|---:|---|---|---|
| 0001 | 雲の骨組みで、初めて島へ降りた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0002 | 雲の骨組みで、初めて島へ降りた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0003 | 雲の骨組みで、初めて島へ降りた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0004 | 雲の骨組みで、初めて島へ降りた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0005 | 雲の骨組みで、初めて島へ降りた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0006 | 雲の骨組みで、初めて島へ降りた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0007 | 雲の骨組みで、初めて島へ降りた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0008 | 雲の骨組みで、初めて島へ降りた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0009 | 雲の骨組みで、初めて島へ降りた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0010 | 雲の骨組みで、初めて島へ降りた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0011 | 雲の骨組みで、連続でオーブを集めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0012 | 雲の骨組みで、連続でオーブを集めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0013 | 雲の骨組みで、連続でオーブを集めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0014 | 雲の骨組みで、連続でオーブを集めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0015 | 雲の骨組みで、連続でオーブを集めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0016 | 雲の骨組みで、連続でオーブを集めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0017 | 雲の骨組みで、連続でオーブを集めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0018 | 雲の骨組みで、連続でオーブを集めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0019 | 雲の骨組みで、連続でオーブを集めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0020 | 雲の骨組みで、連続でオーブを集めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0021 | 雲の骨組みで、基礎を修復した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0022 | 雲の骨組みで、基礎を修復した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0023 | 雲の骨組みで、基礎を修復した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0024 | 雲の骨組みで、基礎を修復した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0025 | 雲の骨組みで、基礎を修復した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0026 | 雲の骨組みで、基礎を修復した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0027 | 雲の骨組みで、基礎を修復した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0028 | 雲の骨組みで、基礎を修復した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0029 | 雲の骨組みで、基礎を修復した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0030 | 雲の骨組みで、基礎を修復した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0031 | 雲の骨組みで、チェックポイントを灯した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0032 | 雲の骨組みで、チェックポイントを灯した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0033 | 雲の骨組みで、チェックポイントを灯した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0034 | 雲の骨組みで、チェックポイントを灯した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0035 | 雲の骨組みで、チェックポイントを灯した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0036 | 雲の骨組みで、チェックポイントを灯した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0037 | 雲の骨組みで、チェックポイントを灯した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0038 | 雲の骨組みで、チェックポイントを灯した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0039 | 雲の骨組みで、チェックポイントを灯した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0040 | 雲の骨組みで、チェックポイントを灯した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0041 | 雲の骨組みで、地上ダッシュを決めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0042 | 雲の骨組みで、地上ダッシュを決めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0043 | 雲の骨組みで、地上ダッシュを決めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0044 | 雲の骨組みで、地上ダッシュを決めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0045 | 雲の骨組みで、地上ダッシュを決めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0046 | 雲の骨組みで、地上ダッシュを決めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0047 | 雲の骨組みで、地上ダッシュを決めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0048 | 雲の骨組みで、地上ダッシュを決めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0049 | 雲の骨組みで、地上ダッシュを決めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0050 | 雲の骨組みで、地上ダッシュを決めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0051 | 雲の骨組みで、空中でAir Dashした時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0052 | 雲の骨組みで、空中でAir Dashした時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0053 | 雲の骨組みで、空中でAir Dashした時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0054 | 雲の骨組みで、空中でAir Dashした時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0055 | 雲の骨組みで、空中でAir Dashした時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0056 | 雲の骨組みで、空中でAir Dashした時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0057 | 雲の骨組みで、空中でAir Dashした時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0058 | 雲の骨組みで、空中でAir Dashした時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0059 | 雲の骨組みで、空中でAir Dashした時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0060 | 雲の骨組みで、空中でAir Dashした時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0061 | 雲の骨組みで、敵を倒さず通り抜けた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0062 | 雲の骨組みで、敵を倒さず通り抜けた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0063 | 雲の骨組みで、敵を倒さず通り抜けた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0064 | 雲の骨組みで、敵を倒さず通り抜けた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0065 | 雲の骨組みで、敵を倒さず通り抜けた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0066 | 雲の骨組みで、敵を倒さず通り抜けた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0067 | 雲の骨組みで、敵を倒さず通り抜けた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0068 | 雲の骨組みで、敵を倒さず通り抜けた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0069 | 雲の骨組みで、敵を倒さず通り抜けた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0070 | 雲の骨組みで、敵を倒さず通り抜けた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0071 | 雲の骨組みで、記憶の断片へ近づいた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0072 | 雲の骨組みで、記憶の断片へ近づいた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0073 | 雲の骨組みで、記憶の断片へ近づいた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0074 | 雲の骨組みで、記憶の断片へ近づいた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0075 | 雲の骨組みで、記憶の断片へ近づいた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0076 | 雲の骨組みで、記憶の断片へ近づいた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0077 | 雲の骨組みで、記憶の断片へ近づいた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0078 | 雲の骨組みで、記憶の断片へ近づいた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0079 | 雲の骨組みで、記憶の断片へ近づいた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0080 | 雲の骨組みで、記憶の断片へ近づいた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0081 | 雲の骨組みで、ゴールの門を見つけた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0082 | 雲の骨組みで、ゴールの門を見つけた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0083 | 雲の骨組みで、ゴールの門を見つけた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0084 | 雲の骨組みで、ゴールの門を見つけた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0085 | 雲の骨組みで、ゴールの門を見つけた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0086 | 雲の骨組みで、ゴールの門を見つけた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0087 | 雲の骨組みで、ゴールの門を見つけた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0088 | 雲の骨組みで、ゴールの門を見つけた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0089 | 雲の骨組みで、ゴールの門を見つけた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0090 | 雲の骨組みで、ゴールの門を見つけた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0091 | 雲の骨組みで、深い落下から戻った時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0092 | 雲の骨組みで、深い落下から戻った時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0093 | 雲の骨組みで、深い落下から戻った時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0094 | 雲の骨組みで、深い落下から戻った時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0095 | 雲の骨組みで、深い落下から戻った時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0096 | 雲の骨組みで、深い落下から戻った時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0097 | 雲の骨組みで、深い落下から戻った時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0098 | 雲の骨組みで、深い落下から戻った時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0099 | 雲の骨組みで、深い落下から戻った時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0100 | 雲の骨組みで、深い落下から戻った時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0101 | 逆さの滝で、初めて島へ降りた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0102 | 逆さの滝で、初めて島へ降りた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0103 | 逆さの滝で、初めて島へ降りた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0104 | 逆さの滝で、初めて島へ降りた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0105 | 逆さの滝で、初めて島へ降りた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0106 | 逆さの滝で、初めて島へ降りた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0107 | 逆さの滝で、初めて島へ降りた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0108 | 逆さの滝で、初めて島へ降りた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0109 | 逆さの滝で、初めて島へ降りた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0110 | 逆さの滝で、初めて島へ降りた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0111 | 逆さの滝で、連続でオーブを集めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0112 | 逆さの滝で、連続でオーブを集めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0113 | 逆さの滝で、連続でオーブを集めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0114 | 逆さの滝で、連続でオーブを集めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0115 | 逆さの滝で、連続でオーブを集めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0116 | 逆さの滝で、連続でオーブを集めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0117 | 逆さの滝で、連続でオーブを集めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0118 | 逆さの滝で、連続でオーブを集めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0119 | 逆さの滝で、連続でオーブを集めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0120 | 逆さの滝で、連続でオーブを集めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0121 | 逆さの滝で、基礎を修復した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0122 | 逆さの滝で、基礎を修復した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0123 | 逆さの滝で、基礎を修復した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0124 | 逆さの滝で、基礎を修復した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0125 | 逆さの滝で、基礎を修復した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0126 | 逆さの滝で、基礎を修復した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0127 | 逆さの滝で、基礎を修復した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0128 | 逆さの滝で、基礎を修復した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0129 | 逆さの滝で、基礎を修復した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0130 | 逆さの滝で、基礎を修復した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0131 | 逆さの滝で、チェックポイントを灯した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0132 | 逆さの滝で、チェックポイントを灯した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0133 | 逆さの滝で、チェックポイントを灯した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0134 | 逆さの滝で、チェックポイントを灯した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0135 | 逆さの滝で、チェックポイントを灯した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0136 | 逆さの滝で、チェックポイントを灯した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0137 | 逆さの滝で、チェックポイントを灯した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0138 | 逆さの滝で、チェックポイントを灯した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0139 | 逆さの滝で、チェックポイントを灯した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0140 | 逆さの滝で、チェックポイントを灯した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0141 | 逆さの滝で、地上ダッシュを決めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0142 | 逆さの滝で、地上ダッシュを決めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0143 | 逆さの滝で、地上ダッシュを決めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0144 | 逆さの滝で、地上ダッシュを決めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0145 | 逆さの滝で、地上ダッシュを決めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0146 | 逆さの滝で、地上ダッシュを決めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0147 | 逆さの滝で、地上ダッシュを決めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0148 | 逆さの滝で、地上ダッシュを決めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0149 | 逆さの滝で、地上ダッシュを決めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0150 | 逆さの滝で、地上ダッシュを決めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0151 | 逆さの滝で、空中でAir Dashした時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0152 | 逆さの滝で、空中でAir Dashした時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0153 | 逆さの滝で、空中でAir Dashした時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0154 | 逆さの滝で、空中でAir Dashした時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0155 | 逆さの滝で、空中でAir Dashした時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0156 | 逆さの滝で、空中でAir Dashした時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0157 | 逆さの滝で、空中でAir Dashした時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0158 | 逆さの滝で、空中でAir Dashした時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0159 | 逆さの滝で、空中でAir Dashした時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0160 | 逆さの滝で、空中でAir Dashした時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0161 | 逆さの滝で、敵を倒さず通り抜けた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0162 | 逆さの滝で、敵を倒さず通り抜けた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0163 | 逆さの滝で、敵を倒さず通り抜けた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0164 | 逆さの滝で、敵を倒さず通り抜けた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0165 | 逆さの滝で、敵を倒さず通り抜けた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0166 | 逆さの滝で、敵を倒さず通り抜けた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0167 | 逆さの滝で、敵を倒さず通り抜けた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0168 | 逆さの滝で、敵を倒さず通り抜けた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0169 | 逆さの滝で、敵を倒さず通り抜けた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0170 | 逆さの滝で、敵を倒さず通り抜けた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0171 | 逆さの滝で、記憶の断片へ近づいた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0172 | 逆さの滝で、記憶の断片へ近づいた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0173 | 逆さの滝で、記憶の断片へ近づいた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0174 | 逆さの滝で、記憶の断片へ近づいた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0175 | 逆さの滝で、記憶の断片へ近づいた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0176 | 逆さの滝で、記憶の断片へ近づいた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0177 | 逆さの滝で、記憶の断片へ近づいた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0178 | 逆さの滝で、記憶の断片へ近づいた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0179 | 逆さの滝で、記憶の断片へ近づいた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0180 | 逆さの滝で、記憶の断片へ近づいた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0181 | 逆さの滝で、ゴールの門を見つけた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0182 | 逆さの滝で、ゴールの門を見つけた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0183 | 逆さの滝で、ゴールの門を見つけた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0184 | 逆さの滝で、ゴールの門を見つけた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0185 | 逆さの滝で、ゴールの門を見つけた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0186 | 逆さの滝で、ゴールの門を見つけた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0187 | 逆さの滝で、ゴールの門を見つけた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0188 | 逆さの滝で、ゴールの門を見つけた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0189 | 逆さの滝で、ゴールの門を見つけた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0190 | 逆さの滝で、ゴールの門を見つけた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0191 | 逆さの滝で、深い落下から戻った時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0192 | 逆さの滝で、深い落下から戻った時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0193 | 逆さの滝で、深い落下から戻った時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0194 | 逆さの滝で、深い落下から戻った時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0195 | 逆さの滝で、深い落下から戻った時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0196 | 逆さの滝で、深い落下から戻った時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0197 | 逆さの滝で、深い落下から戻った時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0198 | 逆さの滝で、深い落下から戻った時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0199 | 逆さの滝で、深い落下から戻った時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0200 | 逆さの滝で、深い落下から戻った時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0201 | 月光の湖で、初めて島へ降りた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0202 | 月光の湖で、初めて島へ降りた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0203 | 月光の湖で、初めて島へ降りた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0204 | 月光の湖で、初めて島へ降りた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0205 | 月光の湖で、初めて島へ降りた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0206 | 月光の湖で、初めて島へ降りた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0207 | 月光の湖で、初めて島へ降りた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0208 | 月光の湖で、初めて島へ降りた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0209 | 月光の湖で、初めて島へ降りた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0210 | 月光の湖で、初めて島へ降りた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0211 | 月光の湖で、連続でオーブを集めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0212 | 月光の湖で、連続でオーブを集めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0213 | 月光の湖で、連続でオーブを集めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0214 | 月光の湖で、連続でオーブを集めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0215 | 月光の湖で、連続でオーブを集めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0216 | 月光の湖で、連続でオーブを集めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0217 | 月光の湖で、連続でオーブを集めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0218 | 月光の湖で、連続でオーブを集めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0219 | 月光の湖で、連続でオーブを集めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0220 | 月光の湖で、連続でオーブを集めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0221 | 月光の湖で、基礎を修復した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0222 | 月光の湖で、基礎を修復した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0223 | 月光の湖で、基礎を修復した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0224 | 月光の湖で、基礎を修復した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0225 | 月光の湖で、基礎を修復した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0226 | 月光の湖で、基礎を修復した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0227 | 月光の湖で、基礎を修復した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0228 | 月光の湖で、基礎を修復した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0229 | 月光の湖で、基礎を修復した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0230 | 月光の湖で、基礎を修復した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0231 | 月光の湖で、チェックポイントを灯した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0232 | 月光の湖で、チェックポイントを灯した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0233 | 月光の湖で、チェックポイントを灯した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0234 | 月光の湖で、チェックポイントを灯した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0235 | 月光の湖で、チェックポイントを灯した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0236 | 月光の湖で、チェックポイントを灯した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0237 | 月光の湖で、チェックポイントを灯した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0238 | 月光の湖で、チェックポイントを灯した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0239 | 月光の湖で、チェックポイントを灯した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0240 | 月光の湖で、チェックポイントを灯した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0241 | 月光の湖で、地上ダッシュを決めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0242 | 月光の湖で、地上ダッシュを決めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0243 | 月光の湖で、地上ダッシュを決めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0244 | 月光の湖で、地上ダッシュを決めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0245 | 月光の湖で、地上ダッシュを決めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0246 | 月光の湖で、地上ダッシュを決めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0247 | 月光の湖で、地上ダッシュを決めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0248 | 月光の湖で、地上ダッシュを決めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0249 | 月光の湖で、地上ダッシュを決めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0250 | 月光の湖で、地上ダッシュを決めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0251 | 月光の湖で、空中でAir Dashした時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0252 | 月光の湖で、空中でAir Dashした時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0253 | 月光の湖で、空中でAir Dashした時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0254 | 月光の湖で、空中でAir Dashした時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0255 | 月光の湖で、空中でAir Dashした時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0256 | 月光の湖で、空中でAir Dashした時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0257 | 月光の湖で、空中でAir Dashした時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0258 | 月光の湖で、空中でAir Dashした時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0259 | 月光の湖で、空中でAir Dashした時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0260 | 月光の湖で、空中でAir Dashした時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0261 | 月光の湖で、敵を倒さず通り抜けた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0262 | 月光の湖で、敵を倒さず通り抜けた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0263 | 月光の湖で、敵を倒さず通り抜けた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0264 | 月光の湖で、敵を倒さず通り抜けた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0265 | 月光の湖で、敵を倒さず通り抜けた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0266 | 月光の湖で、敵を倒さず通り抜けた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0267 | 月光の湖で、敵を倒さず通り抜けた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0268 | 月光の湖で、敵を倒さず通り抜けた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0269 | 月光の湖で、敵を倒さず通り抜けた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0270 | 月光の湖で、敵を倒さず通り抜けた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0271 | 月光の湖で、記憶の断片へ近づいた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0272 | 月光の湖で、記憶の断片へ近づいた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0273 | 月光の湖で、記憶の断片へ近づいた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0274 | 月光の湖で、記憶の断片へ近づいた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0275 | 月光の湖で、記憶の断片へ近づいた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0276 | 月光の湖で、記憶の断片へ近づいた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0277 | 月光の湖で、記憶の断片へ近づいた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0278 | 月光の湖で、記憶の断片へ近づいた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0279 | 月光の湖で、記憶の断片へ近づいた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0280 | 月光の湖で、記憶の断片へ近づいた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0281 | 月光の湖で、ゴールの門を見つけた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0282 | 月光の湖で、ゴールの門を見つけた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0283 | 月光の湖で、ゴールの門を見つけた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0284 | 月光の湖で、ゴールの門を見つけた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0285 | 月光の湖で、ゴールの門を見つけた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0286 | 月光の湖で、ゴールの門を見つけた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0287 | 月光の湖で、ゴールの門を見つけた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0288 | 月光の湖で、ゴールの門を見つけた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0289 | 月光の湖で、ゴールの門を見つけた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0290 | 月光の湖で、ゴールの門を見つけた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0291 | 月光の湖で、深い落下から戻った時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0292 | 月光の湖で、深い落下から戻った時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0293 | 月光の湖で、深い落下から戻った時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0294 | 月光の湖で、深い落下から戻った時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0295 | 月光の湖で、深い落下から戻った時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0296 | 月光の湖で、深い落下から戻った時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0297 | 月光の湖で、深い落下から戻った時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0298 | 月光の湖で、深い落下から戻った時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0299 | 月光の湖で、深い落下から戻った時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0300 | 月光の湖で、深い落下から戻った時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0301 | 眠る機械島で、初めて島へ降りた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0302 | 眠る機械島で、初めて島へ降りた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0303 | 眠る機械島で、初めて島へ降りた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0304 | 眠る機械島で、初めて島へ降りた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0305 | 眠る機械島で、初めて島へ降りた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0306 | 眠る機械島で、初めて島へ降りた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0307 | 眠る機械島で、初めて島へ降りた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0308 | 眠る機械島で、初めて島へ降りた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0309 | 眠る機械島で、初めて島へ降りた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0310 | 眠る機械島で、初めて島へ降りた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0311 | 眠る機械島で、連続でオーブを集めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0312 | 眠る機械島で、連続でオーブを集めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0313 | 眠る機械島で、連続でオーブを集めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0314 | 眠る機械島で、連続でオーブを集めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0315 | 眠る機械島で、連続でオーブを集めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0316 | 眠る機械島で、連続でオーブを集めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0317 | 眠る機械島で、連続でオーブを集めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0318 | 眠る機械島で、連続でオーブを集めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0319 | 眠る機械島で、連続でオーブを集めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0320 | 眠る機械島で、連続でオーブを集めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0321 | 眠る機械島で、基礎を修復した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0322 | 眠る機械島で、基礎を修復した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0323 | 眠る機械島で、基礎を修復した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0324 | 眠る機械島で、基礎を修復した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0325 | 眠る機械島で、基礎を修復した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0326 | 眠る機械島で、基礎を修復した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0327 | 眠る機械島で、基礎を修復した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0328 | 眠る機械島で、基礎を修復した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0329 | 眠る機械島で、基礎を修復した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0330 | 眠る機械島で、基礎を修復した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0331 | 眠る機械島で、チェックポイントを灯した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0332 | 眠る機械島で、チェックポイントを灯した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0333 | 眠る機械島で、チェックポイントを灯した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0334 | 眠る機械島で、チェックポイントを灯した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0335 | 眠る機械島で、チェックポイントを灯した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0336 | 眠る機械島で、チェックポイントを灯した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0337 | 眠る機械島で、チェックポイントを灯した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0338 | 眠る機械島で、チェックポイントを灯した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0339 | 眠る機械島で、チェックポイントを灯した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0340 | 眠る機械島で、チェックポイントを灯した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0341 | 眠る機械島で、地上ダッシュを決めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0342 | 眠る機械島で、地上ダッシュを決めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0343 | 眠る機械島で、地上ダッシュを決めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0344 | 眠る機械島で、地上ダッシュを決めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0345 | 眠る機械島で、地上ダッシュを決めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0346 | 眠る機械島で、地上ダッシュを決めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0347 | 眠る機械島で、地上ダッシュを決めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0348 | 眠る機械島で、地上ダッシュを決めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0349 | 眠る機械島で、地上ダッシュを決めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0350 | 眠る機械島で、地上ダッシュを決めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0351 | 眠る機械島で、空中でAir Dashした時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0352 | 眠る機械島で、空中でAir Dashした時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0353 | 眠る機械島で、空中でAir Dashした時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0354 | 眠る機械島で、空中でAir Dashした時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0355 | 眠る機械島で、空中でAir Dashした時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0356 | 眠る機械島で、空中でAir Dashした時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0357 | 眠る機械島で、空中でAir Dashした時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0358 | 眠る機械島で、空中でAir Dashした時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0359 | 眠る機械島で、空中でAir Dashした時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0360 | 眠る機械島で、空中でAir Dashした時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0361 | 眠る機械島で、敵を倒さず通り抜けた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0362 | 眠る機械島で、敵を倒さず通り抜けた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0363 | 眠る機械島で、敵を倒さず通り抜けた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0364 | 眠る機械島で、敵を倒さず通り抜けた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0365 | 眠る機械島で、敵を倒さず通り抜けた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0366 | 眠る機械島で、敵を倒さず通り抜けた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0367 | 眠る機械島で、敵を倒さず通り抜けた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0368 | 眠る機械島で、敵を倒さず通り抜けた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0369 | 眠る機械島で、敵を倒さず通り抜けた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0370 | 眠る機械島で、敵を倒さず通り抜けた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0371 | 眠る機械島で、記憶の断片へ近づいた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0372 | 眠る機械島で、記憶の断片へ近づいた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0373 | 眠る機械島で、記憶の断片へ近づいた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0374 | 眠る機械島で、記憶の断片へ近づいた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0375 | 眠る機械島で、記憶の断片へ近づいた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0376 | 眠る機械島で、記憶の断片へ近づいた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0377 | 眠る機械島で、記憶の断片へ近づいた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0378 | 眠る機械島で、記憶の断片へ近づいた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0379 | 眠る機械島で、記憶の断片へ近づいた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0380 | 眠る機械島で、記憶の断片へ近づいた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0381 | 眠る機械島で、ゴールの門を見つけた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0382 | 眠る機械島で、ゴールの門を見つけた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0383 | 眠る機械島で、ゴールの門を見つけた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0384 | 眠る機械島で、ゴールの門を見つけた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0385 | 眠る機械島で、ゴールの門を見つけた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0386 | 眠る機械島で、ゴールの門を見つけた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0387 | 眠る機械島で、ゴールの門を見つけた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0388 | 眠る機械島で、ゴールの門を見つけた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0389 | 眠る機械島で、ゴールの門を見つけた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0390 | 眠る機械島で、ゴールの門を見つけた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0391 | 眠る機械島で、深い落下から戻った時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0392 | 眠る機械島で、深い落下から戻った時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0393 | 眠る機械島で、深い落下から戻った時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0394 | 眠る機械島で、深い落下から戻った時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0395 | 眠る機械島で、深い落下から戻った時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0396 | 眠る機械島で、深い落下から戻った時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0397 | 眠る機械島で、深い落下から戻った時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0398 | 眠る機械島で、深い落下から戻った時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0399 | 眠る機械島で、深い落下から戻った時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0400 | 眠る機械島で、深い落下から戻った時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0401 | ガラスの峡谷で、初めて島へ降りた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0402 | ガラスの峡谷で、初めて島へ降りた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0403 | ガラスの峡谷で、初めて島へ降りた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0404 | ガラスの峡谷で、初めて島へ降りた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0405 | ガラスの峡谷で、初めて島へ降りた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0406 | ガラスの峡谷で、初めて島へ降りた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0407 | ガラスの峡谷で、初めて島へ降りた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0408 | ガラスの峡谷で、初めて島へ降りた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0409 | ガラスの峡谷で、初めて島へ降りた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0410 | ガラスの峡谷で、初めて島へ降りた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0411 | ガラスの峡谷で、連続でオーブを集めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0412 | ガラスの峡谷で、連続でオーブを集めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0413 | ガラスの峡谷で、連続でオーブを集めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0414 | ガラスの峡谷で、連続でオーブを集めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0415 | ガラスの峡谷で、連続でオーブを集めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0416 | ガラスの峡谷で、連続でオーブを集めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0417 | ガラスの峡谷で、連続でオーブを集めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0418 | ガラスの峡谷で、連続でオーブを集めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0419 | ガラスの峡谷で、連続でオーブを集めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0420 | ガラスの峡谷で、連続でオーブを集めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0421 | ガラスの峡谷で、基礎を修復した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0422 | ガラスの峡谷で、基礎を修復した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0423 | ガラスの峡谷で、基礎を修復した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0424 | ガラスの峡谷で、基礎を修復した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0425 | ガラスの峡谷で、基礎を修復した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0426 | ガラスの峡谷で、基礎を修復した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0427 | ガラスの峡谷で、基礎を修復した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0428 | ガラスの峡谷で、基礎を修復した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0429 | ガラスの峡谷で、基礎を修復した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0430 | ガラスの峡谷で、基礎を修復した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0431 | ガラスの峡谷で、チェックポイントを灯した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0432 | ガラスの峡谷で、チェックポイントを灯した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0433 | ガラスの峡谷で、チェックポイントを灯した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0434 | ガラスの峡谷で、チェックポイントを灯した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0435 | ガラスの峡谷で、チェックポイントを灯した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0436 | ガラスの峡谷で、チェックポイントを灯した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0437 | ガラスの峡谷で、チェックポイントを灯した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0438 | ガラスの峡谷で、チェックポイントを灯した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0439 | ガラスの峡谷で、チェックポイントを灯した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0440 | ガラスの峡谷で、チェックポイントを灯した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0441 | ガラスの峡谷で、地上ダッシュを決めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0442 | ガラスの峡谷で、地上ダッシュを決めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0443 | ガラスの峡谷で、地上ダッシュを決めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0444 | ガラスの峡谷で、地上ダッシュを決めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0445 | ガラスの峡谷で、地上ダッシュを決めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0446 | ガラスの峡谷で、地上ダッシュを決めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0447 | ガラスの峡谷で、地上ダッシュを決めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0448 | ガラスの峡谷で、地上ダッシュを決めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0449 | ガラスの峡谷で、地上ダッシュを決めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0450 | ガラスの峡谷で、地上ダッシュを決めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0451 | ガラスの峡谷で、空中でAir Dashした時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0452 | ガラスの峡谷で、空中でAir Dashした時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0453 | ガラスの峡谷で、空中でAir Dashした時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0454 | ガラスの峡谷で、空中でAir Dashした時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0455 | ガラスの峡谷で、空中でAir Dashした時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0456 | ガラスの峡谷で、空中でAir Dashした時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0457 | ガラスの峡谷で、空中でAir Dashした時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0458 | ガラスの峡谷で、空中でAir Dashした時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0459 | ガラスの峡谷で、空中でAir Dashした時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0460 | ガラスの峡谷で、空中でAir Dashした時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0461 | ガラスの峡谷で、敵を倒さず通り抜けた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0462 | ガラスの峡谷で、敵を倒さず通り抜けた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0463 | ガラスの峡谷で、敵を倒さず通り抜けた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0464 | ガラスの峡谷で、敵を倒さず通り抜けた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0465 | ガラスの峡谷で、敵を倒さず通り抜けた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0466 | ガラスの峡谷で、敵を倒さず通り抜けた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0467 | ガラスの峡谷で、敵を倒さず通り抜けた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0468 | ガラスの峡谷で、敵を倒さず通り抜けた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0469 | ガラスの峡谷で、敵を倒さず通り抜けた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0470 | ガラスの峡谷で、敵を倒さず通り抜けた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0471 | ガラスの峡谷で、記憶の断片へ近づいた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0472 | ガラスの峡谷で、記憶の断片へ近づいた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0473 | ガラスの峡谷で、記憶の断片へ近づいた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0474 | ガラスの峡谷で、記憶の断片へ近づいた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0475 | ガラスの峡谷で、記憶の断片へ近づいた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0476 | ガラスの峡谷で、記憶の断片へ近づいた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0477 | ガラスの峡谷で、記憶の断片へ近づいた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0478 | ガラスの峡谷で、記憶の断片へ近づいた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0479 | ガラスの峡谷で、記憶の断片へ近づいた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0480 | ガラスの峡谷で、記憶の断片へ近づいた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0481 | ガラスの峡谷で、ゴールの門を見つけた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0482 | ガラスの峡谷で、ゴールの門を見つけた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0483 | ガラスの峡谷で、ゴールの門を見つけた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0484 | ガラスの峡谷で、ゴールの門を見つけた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0485 | ガラスの峡谷で、ゴールの門を見つけた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0486 | ガラスの峡谷で、ゴールの門を見つけた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0487 | ガラスの峡谷で、ゴールの門を見つけた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0488 | ガラスの峡谷で、ゴールの門を見つけた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0489 | ガラスの峡谷で、ゴールの門を見つけた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0490 | ガラスの峡谷で、ゴールの門を見つけた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0491 | ガラスの峡谷で、深い落下から戻った時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0492 | ガラスの峡谷で、深い落下から戻った時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0493 | ガラスの峡谷で、深い落下から戻った時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0494 | ガラスの峡谷で、深い落下から戻った時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0495 | ガラスの峡谷で、深い落下から戻った時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0496 | ガラスの峡谷で、深い落下から戻った時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0497 | ガラスの峡谷で、深い落下から戻った時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0498 | ガラスの峡谷で、深い落下から戻った時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0499 | ガラスの峡谷で、深い落下から戻った時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0500 | ガラスの峡谷で、深い落下から戻った時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0501 | 風化した尖塔で、初めて島へ降りた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0502 | 風化した尖塔で、初めて島へ降りた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0503 | 風化した尖塔で、初めて島へ降りた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0504 | 風化した尖塔で、初めて島へ降りた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0505 | 風化した尖塔で、初めて島へ降りた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0506 | 風化した尖塔で、初めて島へ降りた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0507 | 風化した尖塔で、初めて島へ降りた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0508 | 風化した尖塔で、初めて島へ降りた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0509 | 風化した尖塔で、初めて島へ降りた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0510 | 風化した尖塔で、初めて島へ降りた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0511 | 風化した尖塔で、連続でオーブを集めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0512 | 風化した尖塔で、連続でオーブを集めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0513 | 風化した尖塔で、連続でオーブを集めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0514 | 風化した尖塔で、連続でオーブを集めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0515 | 風化した尖塔で、連続でオーブを集めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0516 | 風化した尖塔で、連続でオーブを集めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0517 | 風化した尖塔で、連続でオーブを集めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0518 | 風化した尖塔で、連続でオーブを集めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0519 | 風化した尖塔で、連続でオーブを集めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0520 | 風化した尖塔で、連続でオーブを集めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0521 | 風化した尖塔で、基礎を修復した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0522 | 風化した尖塔で、基礎を修復した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0523 | 風化した尖塔で、基礎を修復した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0524 | 風化した尖塔で、基礎を修復した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0525 | 風化した尖塔で、基礎を修復した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0526 | 風化した尖塔で、基礎を修復した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0527 | 風化した尖塔で、基礎を修復した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0528 | 風化した尖塔で、基礎を修復した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0529 | 風化した尖塔で、基礎を修復した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0530 | 風化した尖塔で、基礎を修復した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0531 | 風化した尖塔で、チェックポイントを灯した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0532 | 風化した尖塔で、チェックポイントを灯した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0533 | 風化した尖塔で、チェックポイントを灯した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0534 | 風化した尖塔で、チェックポイントを灯した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0535 | 風化した尖塔で、チェックポイントを灯した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0536 | 風化した尖塔で、チェックポイントを灯した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0537 | 風化した尖塔で、チェックポイントを灯した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0538 | 風化した尖塔で、チェックポイントを灯した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0539 | 風化した尖塔で、チェックポイントを灯した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0540 | 風化した尖塔で、チェックポイントを灯した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0541 | 風化した尖塔で、地上ダッシュを決めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0542 | 風化した尖塔で、地上ダッシュを決めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0543 | 風化した尖塔で、地上ダッシュを決めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0544 | 風化した尖塔で、地上ダッシュを決めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0545 | 風化した尖塔で、地上ダッシュを決めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0546 | 風化した尖塔で、地上ダッシュを決めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0547 | 風化した尖塔で、地上ダッシュを決めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0548 | 風化した尖塔で、地上ダッシュを決めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0549 | 風化した尖塔で、地上ダッシュを決めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0550 | 風化した尖塔で、地上ダッシュを決めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0551 | 風化した尖塔で、空中でAir Dashした時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0552 | 風化した尖塔で、空中でAir Dashした時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0553 | 風化した尖塔で、空中でAir Dashした時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0554 | 風化した尖塔で、空中でAir Dashした時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0555 | 風化した尖塔で、空中でAir Dashした時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0556 | 風化した尖塔で、空中でAir Dashした時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0557 | 風化した尖塔で、空中でAir Dashした時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0558 | 風化した尖塔で、空中でAir Dashした時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0559 | 風化した尖塔で、空中でAir Dashした時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0560 | 風化した尖塔で、空中でAir Dashした時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0561 | 風化した尖塔で、敵を倒さず通り抜けた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0562 | 風化した尖塔で、敵を倒さず通り抜けた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0563 | 風化した尖塔で、敵を倒さず通り抜けた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0564 | 風化した尖塔で、敵を倒さず通り抜けた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0565 | 風化した尖塔で、敵を倒さず通り抜けた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0566 | 風化した尖塔で、敵を倒さず通り抜けた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0567 | 風化した尖塔で、敵を倒さず通り抜けた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0568 | 風化した尖塔で、敵を倒さず通り抜けた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0569 | 風化した尖塔で、敵を倒さず通り抜けた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0570 | 風化した尖塔で、敵を倒さず通り抜けた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0571 | 風化した尖塔で、記憶の断片へ近づいた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0572 | 風化した尖塔で、記憶の断片へ近づいた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0573 | 風化した尖塔で、記憶の断片へ近づいた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0574 | 風化した尖塔で、記憶の断片へ近づいた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0575 | 風化した尖塔で、記憶の断片へ近づいた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0576 | 風化した尖塔で、記憶の断片へ近づいた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0577 | 風化した尖塔で、記憶の断片へ近づいた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0578 | 風化した尖塔で、記憶の断片へ近づいた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0579 | 風化した尖塔で、記憶の断片へ近づいた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0580 | 風化した尖塔で、記憶の断片へ近づいた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0581 | 風化した尖塔で、ゴールの門を見つけた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0582 | 風化した尖塔で、ゴールの門を見つけた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0583 | 風化した尖塔で、ゴールの門を見つけた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0584 | 風化した尖塔で、ゴールの門を見つけた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0585 | 風化した尖塔で、ゴールの門を見つけた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0586 | 風化した尖塔で、ゴールの門を見つけた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0587 | 風化した尖塔で、ゴールの門を見つけた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0588 | 風化した尖塔で、ゴールの門を見つけた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0589 | 風化した尖塔で、ゴールの門を見つけた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0590 | 風化した尖塔で、ゴールの門を見つけた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0591 | 風化した尖塔で、深い落下から戻った時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0592 | 風化した尖塔で、深い落下から戻った時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0593 | 風化した尖塔で、深い落下から戻った時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0594 | 風化した尖塔で、深い落下から戻った時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0595 | 風化した尖塔で、深い落下から戻った時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0596 | 風化した尖塔で、深い落下から戻った時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0597 | 風化した尖塔で、深い落下から戻った時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0598 | 風化した尖塔で、深い落下から戻った時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0599 | 風化した尖塔で、深い落下から戻った時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0600 | 風化した尖塔で、深い落下から戻った時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0601 | 星屑の工房で、初めて島へ降りた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0602 | 星屑の工房で、初めて島へ降りた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0603 | 星屑の工房で、初めて島へ降りた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0604 | 星屑の工房で、初めて島へ降りた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0605 | 星屑の工房で、初めて島へ降りた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0606 | 星屑の工房で、初めて島へ降りた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0607 | 星屑の工房で、初めて島へ降りた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0608 | 星屑の工房で、初めて島へ降りた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0609 | 星屑の工房で、初めて島へ降りた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0610 | 星屑の工房で、初めて島へ降りた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0611 | 星屑の工房で、連続でオーブを集めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0612 | 星屑の工房で、連続でオーブを集めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0613 | 星屑の工房で、連続でオーブを集めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0614 | 星屑の工房で、連続でオーブを集めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0615 | 星屑の工房で、連続でオーブを集めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0616 | 星屑の工房で、連続でオーブを集めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0617 | 星屑の工房で、連続でオーブを集めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0618 | 星屑の工房で、連続でオーブを集めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0619 | 星屑の工房で、連続でオーブを集めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0620 | 星屑の工房で、連続でオーブを集めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0621 | 星屑の工房で、基礎を修復した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0622 | 星屑の工房で、基礎を修復した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0623 | 星屑の工房で、基礎を修復した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0624 | 星屑の工房で、基礎を修復した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0625 | 星屑の工房で、基礎を修復した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0626 | 星屑の工房で、基礎を修復した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0627 | 星屑の工房で、基礎を修復した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0628 | 星屑の工房で、基礎を修復した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0629 | 星屑の工房で、基礎を修復した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0630 | 星屑の工房で、基礎を修復した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0631 | 星屑の工房で、チェックポイントを灯した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0632 | 星屑の工房で、チェックポイントを灯した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0633 | 星屑の工房で、チェックポイントを灯した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0634 | 星屑の工房で、チェックポイントを灯した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0635 | 星屑の工房で、チェックポイントを灯した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0636 | 星屑の工房で、チェックポイントを灯した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0637 | 星屑の工房で、チェックポイントを灯した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0638 | 星屑の工房で、チェックポイントを灯した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0639 | 星屑の工房で、チェックポイントを灯した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0640 | 星屑の工房で、チェックポイントを灯した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0641 | 星屑の工房で、地上ダッシュを決めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0642 | 星屑の工房で、地上ダッシュを決めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0643 | 星屑の工房で、地上ダッシュを決めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0644 | 星屑の工房で、地上ダッシュを決めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0645 | 星屑の工房で、地上ダッシュを決めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0646 | 星屑の工房で、地上ダッシュを決めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0647 | 星屑の工房で、地上ダッシュを決めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0648 | 星屑の工房で、地上ダッシュを決めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0649 | 星屑の工房で、地上ダッシュを決めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0650 | 星屑の工房で、地上ダッシュを決めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0651 | 星屑の工房で、空中でAir Dashした時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0652 | 星屑の工房で、空中でAir Dashした時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0653 | 星屑の工房で、空中でAir Dashした時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0654 | 星屑の工房で、空中でAir Dashした時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0655 | 星屑の工房で、空中でAir Dashした時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0656 | 星屑の工房で、空中でAir Dashした時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0657 | 星屑の工房で、空中でAir Dashした時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0658 | 星屑の工房で、空中でAir Dashした時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0659 | 星屑の工房で、空中でAir Dashした時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0660 | 星屑の工房で、空中でAir Dashした時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0661 | 星屑の工房で、敵を倒さず通り抜けた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0662 | 星屑の工房で、敵を倒さず通り抜けた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0663 | 星屑の工房で、敵を倒さず通り抜けた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0664 | 星屑の工房で、敵を倒さず通り抜けた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0665 | 星屑の工房で、敵を倒さず通り抜けた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0666 | 星屑の工房で、敵を倒さず通り抜けた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0667 | 星屑の工房で、敵を倒さず通り抜けた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0668 | 星屑の工房で、敵を倒さず通り抜けた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0669 | 星屑の工房で、敵を倒さず通り抜けた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0670 | 星屑の工房で、敵を倒さず通り抜けた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0671 | 星屑の工房で、記憶の断片へ近づいた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0672 | 星屑の工房で、記憶の断片へ近づいた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0673 | 星屑の工房で、記憶の断片へ近づいた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0674 | 星屑の工房で、記憶の断片へ近づいた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0675 | 星屑の工房で、記憶の断片へ近づいた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0676 | 星屑の工房で、記憶の断片へ近づいた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0677 | 星屑の工房で、記憶の断片へ近づいた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0678 | 星屑の工房で、記憶の断片へ近づいた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0679 | 星屑の工房で、記憶の断片へ近づいた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0680 | 星屑の工房で、記憶の断片へ近づいた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0681 | 星屑の工房で、ゴールの門を見つけた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0682 | 星屑の工房で、ゴールの門を見つけた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0683 | 星屑の工房で、ゴールの門を見つけた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0684 | 星屑の工房で、ゴールの門を見つけた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0685 | 星屑の工房で、ゴールの門を見つけた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0686 | 星屑の工房で、ゴールの門を見つけた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0687 | 星屑の工房で、ゴールの門を見つけた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0688 | 星屑の工房で、ゴールの門を見つけた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0689 | 星屑の工房で、ゴールの門を見つけた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0690 | 星屑の工房で、ゴールの門を見つけた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0691 | 星屑の工房で、深い落下から戻った時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0692 | 星屑の工房で、深い落下から戻った時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0693 | 星屑の工房で、深い落下から戻った時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0694 | 星屑の工房で、深い落下から戻った時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0695 | 星屑の工房で、深い落下から戻った時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0696 | 星屑の工房で、深い落下から戻った時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0697 | 星屑の工房で、深い落下から戻った時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0698 | 星屑の工房で、深い落下から戻った時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0699 | 星屑の工房で、深い落下から戻った時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0700 | 星屑の工房で、深い落下から戻った時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0701 | 白紙の庭園で、初めて島へ降りた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0702 | 白紙の庭園で、初めて島へ降りた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0703 | 白紙の庭園で、初めて島へ降りた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0704 | 白紙の庭園で、初めて島へ降りた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0705 | 白紙の庭園で、初めて島へ降りた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0706 | 白紙の庭園で、初めて島へ降りた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0707 | 白紙の庭園で、初めて島へ降りた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0708 | 白紙の庭園で、初めて島へ降りた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0709 | 白紙の庭園で、初めて島へ降りた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0710 | 白紙の庭園で、初めて島へ降りた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0711 | 白紙の庭園で、連続でオーブを集めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0712 | 白紙の庭園で、連続でオーブを集めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0713 | 白紙の庭園で、連続でオーブを集めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0714 | 白紙の庭園で、連続でオーブを集めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0715 | 白紙の庭園で、連続でオーブを集めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0716 | 白紙の庭園で、連続でオーブを集めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0717 | 白紙の庭園で、連続でオーブを集めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0718 | 白紙の庭園で、連続でオーブを集めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0719 | 白紙の庭園で、連続でオーブを集めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0720 | 白紙の庭園で、連続でオーブを集めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0721 | 白紙の庭園で、基礎を修復した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0722 | 白紙の庭園で、基礎を修復した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0723 | 白紙の庭園で、基礎を修復した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0724 | 白紙の庭園で、基礎を修復した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0725 | 白紙の庭園で、基礎を修復した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0726 | 白紙の庭園で、基礎を修復した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0727 | 白紙の庭園で、基礎を修復した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0728 | 白紙の庭園で、基礎を修復した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0729 | 白紙の庭園で、基礎を修復した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0730 | 白紙の庭園で、基礎を修復した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0731 | 白紙の庭園で、チェックポイントを灯した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0732 | 白紙の庭園で、チェックポイントを灯した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0733 | 白紙の庭園で、チェックポイントを灯した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0734 | 白紙の庭園で、チェックポイントを灯した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0735 | 白紙の庭園で、チェックポイントを灯した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0736 | 白紙の庭園で、チェックポイントを灯した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0737 | 白紙の庭園で、チェックポイントを灯した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0738 | 白紙の庭園で、チェックポイントを灯した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0739 | 白紙の庭園で、チェックポイントを灯した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0740 | 白紙の庭園で、チェックポイントを灯した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0741 | 白紙の庭園で、地上ダッシュを決めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0742 | 白紙の庭園で、地上ダッシュを決めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0743 | 白紙の庭園で、地上ダッシュを決めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0744 | 白紙の庭園で、地上ダッシュを決めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0745 | 白紙の庭園で、地上ダッシュを決めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0746 | 白紙の庭園で、地上ダッシュを決めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0747 | 白紙の庭園で、地上ダッシュを決めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0748 | 白紙の庭園で、地上ダッシュを決めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0749 | 白紙の庭園で、地上ダッシュを決めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0750 | 白紙の庭園で、地上ダッシュを決めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0751 | 白紙の庭園で、空中でAir Dashした時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0752 | 白紙の庭園で、空中でAir Dashした時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0753 | 白紙の庭園で、空中でAir Dashした時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0754 | 白紙の庭園で、空中でAir Dashした時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0755 | 白紙の庭園で、空中でAir Dashした時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0756 | 白紙の庭園で、空中でAir Dashした時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0757 | 白紙の庭園で、空中でAir Dashした時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0758 | 白紙の庭園で、空中でAir Dashした時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0759 | 白紙の庭園で、空中でAir Dashした時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0760 | 白紙の庭園で、空中でAir Dashした時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0761 | 白紙の庭園で、敵を倒さず通り抜けた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0762 | 白紙の庭園で、敵を倒さず通り抜けた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0763 | 白紙の庭園で、敵を倒さず通り抜けた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0764 | 白紙の庭園で、敵を倒さず通り抜けた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0765 | 白紙の庭園で、敵を倒さず通り抜けた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0766 | 白紙の庭園で、敵を倒さず通り抜けた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0767 | 白紙の庭園で、敵を倒さず通り抜けた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0768 | 白紙の庭園で、敵を倒さず通り抜けた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0769 | 白紙の庭園で、敵を倒さず通り抜けた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0770 | 白紙の庭園で、敵を倒さず通り抜けた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0771 | 白紙の庭園で、記憶の断片へ近づいた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0772 | 白紙の庭園で、記憶の断片へ近づいた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0773 | 白紙の庭園で、記憶の断片へ近づいた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0774 | 白紙の庭園で、記憶の断片へ近づいた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0775 | 白紙の庭園で、記憶の断片へ近づいた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0776 | 白紙の庭園で、記憶の断片へ近づいた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0777 | 白紙の庭園で、記憶の断片へ近づいた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0778 | 白紙の庭園で、記憶の断片へ近づいた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0779 | 白紙の庭園で、記憶の断片へ近づいた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0780 | 白紙の庭園で、記憶の断片へ近づいた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0781 | 白紙の庭園で、ゴールの門を見つけた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0782 | 白紙の庭園で、ゴールの門を見つけた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0783 | 白紙の庭園で、ゴールの門を見つけた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0784 | 白紙の庭園で、ゴールの門を見つけた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0785 | 白紙の庭園で、ゴールの門を見つけた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0786 | 白紙の庭園で、ゴールの門を見つけた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0787 | 白紙の庭園で、ゴールの門を見つけた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0788 | 白紙の庭園で、ゴールの門を見つけた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0789 | 白紙の庭園で、ゴールの門を見つけた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0790 | 白紙の庭園で、ゴールの門を見つけた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0791 | 白紙の庭園で、深い落下から戻った時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0792 | 白紙の庭園で、深い落下から戻った時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0793 | 白紙の庭園で、深い落下から戻った時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0794 | 白紙の庭園で、深い落下から戻った時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0795 | 白紙の庭園で、深い落下から戻った時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0796 | 白紙の庭園で、深い落下から戻った時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0797 | 白紙の庭園で、深い落下から戻った時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0798 | 白紙の庭園で、深い落下から戻った時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0799 | 白紙の庭園で、深い落下から戻った時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0800 | 白紙の庭園で、深い落下から戻った時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0801 | 沈む雲海で、初めて島へ降りた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0802 | 沈む雲海で、初めて島へ降りた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0803 | 沈む雲海で、初めて島へ降りた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0804 | 沈む雲海で、初めて島へ降りた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0805 | 沈む雲海で、初めて島へ降りた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0806 | 沈む雲海で、初めて島へ降りた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0807 | 沈む雲海で、初めて島へ降りた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0808 | 沈む雲海で、初めて島へ降りた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0809 | 沈む雲海で、初めて島へ降りた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0810 | 沈む雲海で、初めて島へ降りた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0811 | 沈む雲海で、連続でオーブを集めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0812 | 沈む雲海で、連続でオーブを集めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0813 | 沈む雲海で、連続でオーブを集めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0814 | 沈む雲海で、連続でオーブを集めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0815 | 沈む雲海で、連続でオーブを集めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0816 | 沈む雲海で、連続でオーブを集めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0817 | 沈む雲海で、連続でオーブを集めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0818 | 沈む雲海で、連続でオーブを集めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0819 | 沈む雲海で、連続でオーブを集めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0820 | 沈む雲海で、連続でオーブを集めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0821 | 沈む雲海で、基礎を修復した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0822 | 沈む雲海で、基礎を修復した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0823 | 沈む雲海で、基礎を修復した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0824 | 沈む雲海で、基礎を修復した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0825 | 沈む雲海で、基礎を修復した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0826 | 沈む雲海で、基礎を修復した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0827 | 沈む雲海で、基礎を修復した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0828 | 沈む雲海で、基礎を修復した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0829 | 沈む雲海で、基礎を修復した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0830 | 沈む雲海で、基礎を修復した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0831 | 沈む雲海で、チェックポイントを灯した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0832 | 沈む雲海で、チェックポイントを灯した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0833 | 沈む雲海で、チェックポイントを灯した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0834 | 沈む雲海で、チェックポイントを灯した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0835 | 沈む雲海で、チェックポイントを灯した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0836 | 沈む雲海で、チェックポイントを灯した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0837 | 沈む雲海で、チェックポイントを灯した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0838 | 沈む雲海で、チェックポイントを灯した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0839 | 沈む雲海で、チェックポイントを灯した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0840 | 沈む雲海で、チェックポイントを灯した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0841 | 沈む雲海で、地上ダッシュを決めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0842 | 沈む雲海で、地上ダッシュを決めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0843 | 沈む雲海で、地上ダッシュを決めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0844 | 沈む雲海で、地上ダッシュを決めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0845 | 沈む雲海で、地上ダッシュを決めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0846 | 沈む雲海で、地上ダッシュを決めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0847 | 沈む雲海で、地上ダッシュを決めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0848 | 沈む雲海で、地上ダッシュを決めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0849 | 沈む雲海で、地上ダッシュを決めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0850 | 沈む雲海で、地上ダッシュを決めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0851 | 沈む雲海で、空中でAir Dashした時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0852 | 沈む雲海で、空中でAir Dashした時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0853 | 沈む雲海で、空中でAir Dashした時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0854 | 沈む雲海で、空中でAir Dashした時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0855 | 沈む雲海で、空中でAir Dashした時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0856 | 沈む雲海で、空中でAir Dashした時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0857 | 沈む雲海で、空中でAir Dashした時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0858 | 沈む雲海で、空中でAir Dashした時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0859 | 沈む雲海で、空中でAir Dashした時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0860 | 沈む雲海で、空中でAir Dashした時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0861 | 沈む雲海で、敵を倒さず通り抜けた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0862 | 沈む雲海で、敵を倒さず通り抜けた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0863 | 沈む雲海で、敵を倒さず通り抜けた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0864 | 沈む雲海で、敵を倒さず通り抜けた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0865 | 沈む雲海で、敵を倒さず通り抜けた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0866 | 沈む雲海で、敵を倒さず通り抜けた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0867 | 沈む雲海で、敵を倒さず通り抜けた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0868 | 沈む雲海で、敵を倒さず通り抜けた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0869 | 沈む雲海で、敵を倒さず通り抜けた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0870 | 沈む雲海で、敵を倒さず通り抜けた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0871 | 沈む雲海で、記憶の断片へ近づいた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0872 | 沈む雲海で、記憶の断片へ近づいた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0873 | 沈む雲海で、記憶の断片へ近づいた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0874 | 沈む雲海で、記憶の断片へ近づいた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0875 | 沈む雲海で、記憶の断片へ近づいた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0876 | 沈む雲海で、記憶の断片へ近づいた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0877 | 沈む雲海で、記憶の断片へ近づいた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0878 | 沈む雲海で、記憶の断片へ近づいた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0879 | 沈む雲海で、記憶の断片へ近づいた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0880 | 沈む雲海で、記憶の断片へ近づいた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0881 | 沈む雲海で、ゴールの門を見つけた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0882 | 沈む雲海で、ゴールの門を見つけた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0883 | 沈む雲海で、ゴールの門を見つけた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0884 | 沈む雲海で、ゴールの門を見つけた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0885 | 沈む雲海で、ゴールの門を見つけた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0886 | 沈む雲海で、ゴールの門を見つけた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0887 | 沈む雲海で、ゴールの門を見つけた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0888 | 沈む雲海で、ゴールの門を見つけた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0889 | 沈む雲海で、ゴールの門を見つけた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0890 | 沈む雲海で、ゴールの門を見つけた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0891 | 沈む雲海で、深い落下から戻った時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0892 | 沈む雲海で、深い落下から戻った時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0893 | 沈む雲海で、深い落下から戻った時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0894 | 沈む雲海で、深い落下から戻った時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0895 | 沈む雲海で、深い落下から戻った時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0896 | 沈む雲海で、深い落下から戻った時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0897 | 沈む雲海で、深い落下から戻った時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0898 | 沈む雲海で、深い落下から戻った時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0899 | 沈む雲海で、深い落下から戻った時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0900 | 沈む雲海で、深い落下から戻った時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0901 | アルケアの外縁で、初めて島へ降りた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0902 | アルケアの外縁で、初めて島へ降りた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0903 | アルケアの外縁で、初めて島へ降りた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0904 | アルケアの外縁で、初めて島へ降りた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0905 | アルケアの外縁で、初めて島へ降りた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0906 | アルケアの外縁で、初めて島へ降りた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0907 | アルケアの外縁で、初めて島へ降りた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0908 | アルケアの外縁で、初めて島へ降りた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0909 | アルケアの外縁で、初めて島へ降りた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0910 | アルケアの外縁で、初めて島へ降りた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0911 | アルケアの外縁で、連続でオーブを集めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0912 | アルケアの外縁で、連続でオーブを集めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0913 | アルケアの外縁で、連続でオーブを集めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0914 | アルケアの外縁で、連続でオーブを集めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0915 | アルケアの外縁で、連続でオーブを集めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0916 | アルケアの外縁で、連続でオーブを集めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0917 | アルケアの外縁で、連続でオーブを集めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0918 | アルケアの外縁で、連続でオーブを集めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0919 | アルケアの外縁で、連続でオーブを集めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0920 | アルケアの外縁で、連続でオーブを集めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0921 | アルケアの外縁で、基礎を修復した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0922 | アルケアの外縁で、基礎を修復した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0923 | アルケアの外縁で、基礎を修復した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0924 | アルケアの外縁で、基礎を修復した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0925 | アルケアの外縁で、基礎を修復した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0926 | アルケアの外縁で、基礎を修復した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0927 | アルケアの外縁で、基礎を修復した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0928 | アルケアの外縁で、基礎を修復した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0929 | アルケアの外縁で、基礎を修復した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0930 | アルケアの外縁で、基礎を修復した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0931 | アルケアの外縁で、チェックポイントを灯した時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0932 | アルケアの外縁で、チェックポイントを灯した時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0933 | アルケアの外縁で、チェックポイントを灯した時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0934 | アルケアの外縁で、チェックポイントを灯した時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0935 | アルケアの外縁で、チェックポイントを灯した時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0936 | アルケアの外縁で、チェックポイントを灯した時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0937 | アルケアの外縁で、チェックポイントを灯した時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0938 | アルケアの外縁で、チェックポイントを灯した時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0939 | アルケアの外縁で、チェックポイントを灯した時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0940 | アルケアの外縁で、チェックポイントを灯した時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0941 | アルケアの外縁で、地上ダッシュを決めた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0942 | アルケアの外縁で、地上ダッシュを決めた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0943 | アルケアの外縁で、地上ダッシュを決めた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0944 | アルケアの外縁で、地上ダッシュを決めた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0945 | アルケアの外縁で、地上ダッシュを決めた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0946 | アルケアの外縁で、地上ダッシュを決めた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0947 | アルケアの外縁で、地上ダッシュを決めた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0948 | アルケアの外縁で、地上ダッシュを決めた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0949 | アルケアの外縁で、地上ダッシュを決めた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0950 | アルケアの外縁で、地上ダッシュを決めた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0951 | アルケアの外縁で、空中でAir Dashした時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0952 | アルケアの外縁で、空中でAir Dashした時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0953 | アルケアの外縁で、空中でAir Dashした時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0954 | アルケアの外縁で、空中でAir Dashした時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0955 | アルケアの外縁で、空中でAir Dashした時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0956 | アルケアの外縁で、空中でAir Dashした時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0957 | アルケアの外縁で、空中でAir Dashした時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0958 | アルケアの外縁で、空中でAir Dashした時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0959 | アルケアの外縁で、空中でAir Dashした時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0960 | アルケアの外縁で、空中でAir Dashした時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0961 | アルケアの外縁で、敵を倒さず通り抜けた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0962 | アルケアの外縁で、敵を倒さず通り抜けた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0963 | アルケアの外縁で、敵を倒さず通り抜けた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0964 | アルケアの外縁で、敵を倒さず通り抜けた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0965 | アルケアの外縁で、敵を倒さず通り抜けた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0966 | アルケアの外縁で、敵を倒さず通り抜けた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0967 | アルケアの外縁で、敵を倒さず通り抜けた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0968 | アルケアの外縁で、敵を倒さず通り抜けた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0969 | アルケアの外縁で、敵を倒さず通り抜けた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0970 | アルケアの外縁で、敵を倒さず通り抜けた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0971 | アルケアの外縁で、記憶の断片へ近づいた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0972 | アルケアの外縁で、記憶の断片へ近づいた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0973 | アルケアの外縁で、記憶の断片へ近づいた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0974 | アルケアの外縁で、記憶の断片へ近づいた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0975 | アルケアの外縁で、記憶の断片へ近づいた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0976 | アルケアの外縁で、記憶の断片へ近づいた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0977 | アルケアの外縁で、記憶の断片へ近づいた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0978 | アルケアの外縁で、記憶の断片へ近づいた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0979 | アルケアの外縁で、記憶の断片へ近づいた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0980 | アルケアの外縁で、記憶の断片へ近づいた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0981 | アルケアの外縁で、ゴールの門を見つけた時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0982 | アルケアの外縁で、ゴールの門を見つけた時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0983 | アルケアの外縁で、ゴールの門を見つけた時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0984 | アルケアの外縁で、ゴールの門を見つけた時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0985 | アルケアの外縁で、ゴールの門を見つけた時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0986 | アルケアの外縁で、ゴールの門を見つけた時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0987 | アルケアの外縁で、ゴールの門を見つけた時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0988 | アルケアの外縁で、ゴールの門を見つけた時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0989 | アルケアの外縁で、ゴールの門を見つけた時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 0990 | アルケアの外縁で、ゴールの門を見つけた時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
| 0991 | アルケアの外縁で、深い落下から戻った時、足場が次のルートを組み替える。 | stage-layouts.js の各ステージ定義へ任意パラメータを追加し、game.js で FEATURE フラグを確認して適用する。 | 保留: ステージ設計待ち |
| 0992 | アルケアの外縁で、深い落下から戻った時、敵が巡回を変えて安全な隙間を作る。 | config.js の専用フラグと MECHANIC_NUMBERS を使い、既存の一時足場・粒子・敵ステートを組み合わせる。 | 保留: 個別画像・台詞待ち |
| 0993 | アルケアの外縁で、深い落下から戻った時、背景が修復状況を景色として記録する。 | 専用画像が必要なため、assets の個別スプライトを追加してから Canvas 描画へ接続する。 | 候補: 既存システムへ安全に統合可能 |
| 0994 | アルケアの外縁で、深い落下から戻った時、住民の台詞が一文だけ完成する。 | セーブ互換性を保つため、snapshot の任意フィールドとして保存し、未設定時は既存挙動に戻す。 | 候補: Chapter 2以降で検証 |
| 0995 | アルケアの外縁で、深い落下から戻った時、オーブが一時的な道具に姿を変える。 | コース選択の島ノードに表示するため、stage-layouts.js とマップ描画の両方へ状態を渡す。 | 候補: Stage Builder対応時に実装 |
| 0996 | アルケアの外縁で、深い落下から戻った時、重力か風向きが短時間だけ調律される。 | 新規キーを増やさず、既存の E・C・F・X・Shift の文脈操作として実装する。 | 保留: ステージ設計待ち |
| 0997 | アルケアの外縁で、深い落下から戻った時、レンの移動に一度だけ補助が付く。 | 難所専用のため、STAGE_NUMERIC_OVERRIDES でステージごとに数値を明示する。 | 保留: 個別画像・台詞待ち |
| 0998 | アルケアの外縁で、深い落下から戻った時、チェックポイントが次の挑戦を予告する。 | 演出専用のため、進行条件は変えず Canvas の描画レイヤーだけを拡張する。 | 候補: 既存システムへ安全に統合可能 |
| 0999 | アルケアの外縁で、深い落下から戻った時、ゲートの一部が完成して近道が見える。 | 敵・足場との衝突を変えるため、Playwrightで機能ON/OFFの両方を確認してから公開する。 | 候補: Chapter 2以降で検証 |
| 1000 | アルケアの外縁で、深い落下から戻った時、失敗した軌跡が救済ルートとして残る。 | Stage Builder を見据え、JSONの modifier 配列として読み込み可能な形で定義する。 | 候補: Stage Builder対応時に実装 |
